//Number(Integer)
let height = 5;
console.log(`Height : ${height}`);

//Number(float)
let width = 9.17;
console.log(`Width : ${width}`);

//Number(String)
let house = "The Santha's Castle";
console.log(`${house} house width is ${width} cm & height is ${height} cm.`);

//Boolean
let house_Constructed = true; //we can pass 0(false) or 1(true)
if (house_Constructed === true)
  console.log(`The House has been constructed Succesfully`);
else console.log(`The House has been not yet constructed`);
