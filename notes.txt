shrtCut:
=> comment line - ctrl + /
=> vsCode zoom - ctrl + +-
=> open the dev tools - ctrl + shift + i or press [f12] in windows
----------
=> Internet - 2 or more devices that can communicate with each other over a network is known as Internet.
=> Software - a set of instructions given to the computer to perform a specific tasks.
=> Plugins - it gives additional features on top of the software without affecting its core functionality.
=> compiler - converts the source code into machine code and generates a exe file.
=> compile time -- code conversion
    -> the time in which the source code is converted to machine code. 
    -> Code is checked for syntax errors.
=> run time -- code execution
    -> the time in which the program is running, it occurs after the compile time. 
    -> Program is executed and gives output.
    -> logical errors happens here.
=> Arguments pass data; parameters receive data.